import 'package:flutter/material.dart';

/// Defines class that containes borders.
class Borders {
  /// Border for image, that represent user in favorite screen.
  static const favoriteCardBorder = BorderRadius.only(
      bottomLeft: Radius.circular(32), bottomRight: Radius.circular(32));
}
